# 可维护源码模块化实施计划

**目标：** 保留 2.0.10 现有界面与下载行为，使设置、站点规则、下载工具、队列和界面分别由可读源码文件构建，并恢复关键压缩变量名的语义。

**约束：** 不改动 `D:\Download\10漫画—重制版.user.js`、Edge 已安装脚本或 GitHub；仅在 `codex/modular-readable-source` 本地分支构建候选。第三方 ZIP 库可以保留 vendor 快照。现存 `D:\Codex\Code\10Comic-upstream` 源码不是 2.0.10 的同版源，只能作为参考，不能无验证地替换发布代码。

## 步骤

1. 对照发布脚本、格式化核心与本地旧工程，列出模块和缺失行为。先保留现有可通过的 14 项测试及迁移校验作为基线。
2. 在隔离分支引入可读的设置、规则、下载工具、队列和界面源码；确保每个源文件可独立解析。逐个替换现有打包核心的对应部分，并用 Node 测试验证接口和行为。
3. 将可确认用途的单字母应用变量改成语义名；通过作用域感知的修改和测试避免误改对象属性、字符串与第三方库。
4. 生成仅供本地测试的用户脚本。运行干净检出的依赖安装、构建、语法、现有规则与下载测试，并针对 Mangabz、看漫画、Zero 与图片范围运行回归测试。
5. 复核候选脚本与 2.0.10 的差异。若仍有无法证明等价的站点或浏览器行为，不替换安装文件，也不建议直接推送 GitHub `main`；明确列出剩余验证项。
